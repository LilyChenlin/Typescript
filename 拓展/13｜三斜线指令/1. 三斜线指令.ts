// 三斜线指令是包含单个XML标签的单行注释， 仅可放在包含它的文件的最顶端

// 1.  ///<reference path="..."/> 用于声明文件间的依赖
// 三斜线引用以它们在文件里出现的顺序，使用深度优先的方式解析。

// 2. ///<reference types="..."/>
// 仅当在你需要写一个d.ts文件时才使用这个指令。

// 3. ///<reference no-default-lib="true"/>

// 4. /// <amd-module/>

